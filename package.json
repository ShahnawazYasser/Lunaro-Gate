{
  "name": "lunaro-gate",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "web",
    "booth",
    "shared"
  ],
  "scripts": {
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
