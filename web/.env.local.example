DATABASE_URL=
DATABASE_URL_UNPOOLED=
GATE_DEFAULT_BOOTH_ID=
GATE_BASE_PRICE_PKR=
GATE_ADDITIONAL_COPY_PRICE_PKR=
GATE_MAX_PRINTS_PER_CODE=
GATE_CODE_EXPIRY_HOURS=
GATE_RATE_LIMIT_PER_MIN=
# Long random string (e.g. `openssl rand -hex 32`). Must match the Authorization: Bearer header sent by Vercel Cron.
CRON_SECRET=
